# <功能名称> — Design Doc

## 元信息

| 项 | 内容 |
|----|------|
| 功能名称 | |
| 作者 | |
| 创建日期 | YYYY-MM-DD |
| 状态 | `Draft` / `InReview` / `Approved` |
| 风险档位 | `LOW` / `MEDIUM` / `HIGH` |
| 关联 Proposal | [proposals/<slug>-YYYY-MM-DD.md](链接) |

## 背景与目标

### 背景
（为什么需要这个功能？解决什么问题？）

### 目标
（一句话描述要做什么）

### 非目标
（明确本期不做什么）

## 核心规则

> 每条规则必须覆盖：触发条件、前置限制、操作流程、状态变化、资源变化、UI 反馈、异常边界、落地点。

### R-01: <规则名称>

- **触发条件**：
- **前置限制**：
- **操作流程**：
- **状态变化**：
- **资源变化**：
- **UI 反馈**：
- **异常边界**：
- **落地点**：（配置表 / 代码模块 / QA 验证点）

### R-02: <规则名称>

（同上结构）

## 技术方案

### 架构影响
（是否涉及新模块、新服务、新协议？）

### 数据变更
（配置表新增/修改、Proto 变更、数据库变更）

### 接口变更
（新增/修改的消息定义）

## 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| | | | |

## 工作假设

> 标注为「待确认」的假设，在澄清完成前不得作为实现依据。

| 假设 | 状态 | 澄清结论 |
|------|------|----------|
| | 待确认 / 已确认 | |

## 模板使用说明

1. **R-XX**：核心规则编号，从 R-01 开始递增，贯穿整个功能生命周期
2. 每条规则必须覆盖 8 个维度，缺失的维度标注「不适用」并说明原因
3. 工作假设必须在 Proposal 阶段前完成澄清
4. 状态流转：`Draft` → `InReview`（评审中）→ `Approved`（门禁通过）
5. Design Doc 通过后回流到 `tkl_design/design/` 目录
